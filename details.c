#include <stdio.h>
int main(){

    int age;
    int height;
    char grade;
    printf("Enter Age :");
    scanf("%d",&age);
    printf("Enter Height(in meters) :");
    scanf("%d",&height);
    printf("Enter Grade :");
    scanf("%s",&grade);

    return 0;
}